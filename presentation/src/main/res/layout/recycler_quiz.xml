<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="quizData"
            type="com.donghyeon.wordnote.domain.model.QuizData"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/cl_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp">

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/bt_select"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:background="@drawable/dr_border"
            app:layout_constraintDimensionRatio="w, 1:1"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tv_quiz"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@{quizData.title}"
            app:layout_constraintTop_toTopOf="@+id/bt_select"
            app:layout_constraintBottom_toBottomOf="@+id/bt_select"
            app:layout_constraintStart_toStartOf="@+id/bt_select"
            app:layout_constraintEnd_toEndOf="@+id/bt_select"
            tools:text="퀴즈"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
